2026-04-09 本地人脸能力深度清理（进行中）

已完成：
- 移除本地 TFLite 推理残留，`FaceRecognitionManager` 已切到纯服务端模式兼容实现。
- 修复由半清理导致的编译错误，`FaceCheck :app:assembleDebug` 已通过。

本轮新增（ML Kit 清理）：
- 开始移除 `com.google.mlkit` 全部依赖与代码引用。
- `FaceDetectionManager` 改为无 ML Kit 的本地兜底框选实现（输出 `Rect`）。
- 联动改造调用方：`AttendanceActivity`、`StudentSignInActivity`、`FaceEnhancementActivity`、`VerifyFeatureConsistencyActivity`。
- 清理构建配置中的 `mlkit` 依赖与 Proguard 规则。

待确认：
- 验证所有签到路径在“无本地检测模型”下的业务体验是否符合预期。
- 如需进一步收敛，可下线“本地检测/增强”文案与入口，仅保留服务端识别链路。
