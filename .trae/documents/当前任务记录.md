**2026-04-04 联合巡检：FaceCheck 编译复核 + 问题分级 + omniattend-core 人脸迁移规划（已完成）**

1.  **FaceCheck 编译检查（已完成）**
    *   **执行结论 - 2026-04-04**:
        *   使用项目内 JDK11 执行 `:app:assembleDebug --no-daemon`，结果通过（exit code 0）。
        *   本轮未触发新的 Java/Kotlin/XML 编译错误，IDE 诊断返回空。
    *   **构建命令（本轮实测）**:
        *   `$env:JAVA_HOME='z:\android_demo\FaceCheck\.trae\tools\jdk-11\jdk-11.0.30+7'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; .\gradlew :app:assembleDebug --no-daemon`

2.  **项目问题分级（已完成，按严重/中等/一般）**
    *   **严重**
        *   `SessionManager` 存在硬编码 API 密钥：`API_SECRET = "my-secret-api-key"`，属于密钥暴露风险。
        *   `omniattend-core` 前端服务层存在硬编码 API Key 与 demo 口令常量，且被多个请求直接复用，风险高。
    *   **中等**
        *   `StudentFragment` 仍使用模拟学生数据，真实数据库加载逻辑未接通。
        *   `HomeFragment.navigateToStudents()` 未连通学生管理页，仅弹“即将上线”提示。
        *   `WebDAVSyncHelper.performDirectSync()` 为占位实现，当前直接返回成功，可能造成“假同步成功”。
        *   本机 `omniattend-core` 构建环境未就绪（`npm` 命令不可用），影响 Web 端构建验证闭环。
    *   **一般**
        *   API 基础配置（域名/Key）在多个文件重复维护，后续建议集中到统一配置入口。
        *   旧考勤与新 checkin 模型并存，命名语义（如 `confidenceScore`）需要在迁移阶段做一次统一口径梳理。

3.  **omniattend-core 人脸识别相关代码迁移规划（仅标记，不改代码，已完成）**
    *   **已识别的人脸相关保留入口（迁移到云端前）**
        *   Worker 路由：`/api/face/embeddings` GET/POST（当前直接读写 `FaceEmbedding`）。
        *   数据结构：`schema.sql` 中 `FaceEmbedding` 表；`types.ts` 中 `faceEmbeddings`、`confidenceScore` 字段。
        *   文档提及：README 与历史文档中仍有人脸字段与示例描述。
    *   **目标结构精简（规划）**
        *   建议收敛为三层：`services/face/`（云端人脸服务调用）、`worker face routes`（薄路由）、`types/face.ts`（独立类型定义）。
        *   Worker 仅保留任务编排与鉴权，不再直接承载向量格式兼容细节。
    *   **迁移后可删除（规划标记）**
        *   可删除旧路由：`worker.ts` 中 `/api/face/embeddings` 的本地直写分支（由云端接口替代后删除）。
        *   可删除旧字段：`users.faceEmbeddings`（若迁移后不再在本地存 embedding JSON）。
        *   可删除旧示例：`services/dataService.ts` 中仅用于旧“人脸考勤”语义的 mock `confidenceScore` 展示逻辑（迁移后改为云端返回统一评分字段）。
        *   可删除文档旧段：README 中旧本地人脸存储说明与过时示例 SQL/JSON。
    *   **当前约束**
        *   仅完成扫描与规划，不删除任何业务代码，不改接口行为。

4.  **本轮执行命令记录（已更新）**
    *   **FaceCheck 编译通过**
        *   `$env:JAVA_HOME='z:\android_demo\FaceCheck\.trae\tools\jdk-11\jdk-11.0.30+7'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; .\gradlew :app:assembleDebug --no-daemon`
    *   **omniattend-core 构建检查（环境问题）**
        *   `npm run build`
        *   结果：终端提示 `npm` 未识别（CommandNotFoundException），需先安装并配置 Node.js/npm。

---

**2026-04-04 学生端查看详情显隐优化 + 编译通过（已完成）**

1.  **学生“查看已提交”密码答案显隐（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   学生端只读详情中，密码输入框默认密码态显示，新增“显示/隐藏”切换按钮。
        *   交互仅在只读详情生效，不影响“去签到”提交流程。
        *   保持接口与字段不变，仅提升答案可读性与隐私控制体验。
    *   **影响范围**: `dialog_student_checkin_submit.xml`, `StudentCoursesFragment.java`。

2.  **构建验证（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   使用项目内 JDK11 执行 `:app:assembleDebug --no-daemon` 通过。
        *   本轮改动未引入新的编译错误。

---

**2026-04-04 学生端改造后编译复检（已完成）**

1.  **Android 编译复检（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   使用项目内 JDK11 环境执行 `:app:assembleDebug --no-daemon`。
        *   本轮未出现新的 Java/Kotlin/XML 编译错误。
        *   编译结果通过：`BUILD SUCCESSFUL`（exit code 0）。
    *   **影响范围**: 构建验证，无新增业务代码变更。

2.  **后续构建命令（固定）**
    *   **命令**: `$env:JAVA_HOME='z:\android_demo\FaceCheck\.trae\tools\jdk-11\jdk-11.0.30+7'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; .\gradlew :app:assembleDebug --no-daemon`

---

**2026-04-04 构建环境一条龙：按 Gradle 要求配置 Java 11（已完成）**

1.  **按项目 Gradle 配置确认 JDK 版本（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   `app/build.gradle.kts` 明确 `sourceCompatibility/targetCompatibility = 11` 且 `kotlinOptions.jvmTarget = "11"`，本项目构建要求 Java 11。
        *   维持现有 Gradle 配置不变，仅补齐本机 Java 环境。

2.  **JDK11 下载与环境配置自动化（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   下载 Temurin JDK11 压缩包并解压到项目内：`.trae/tools/jdk-11/jdk-11.0.30+7`。
        *   新增一键脚本 `setup-java11.ps1`：自动下载、解压、设置 `JAVA_HOME`（用户级）并在当前终端立即生效。
        *   不修改 `gradle.properties`、`build.gradle.kts`、`settings.gradle.kts` 的既有设定。
    *   **影响范围**: `.trae/tools/setup-java11.ps1`（新增）。

3.  **环境验证（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   终端验证 `java -version` 已返回 `openjdk 11.0.30 (Temurin)`。
        *   `gradlew --version` 调用返回成功（exit code 0），说明 Gradle 已可使用该 Java 环境。

4.  **使用方式（已落地）**
    *   **命令**: `powershell -ExecutionPolicy Bypass -File z:\android_demo\FaceCheck\.trae\tools\setup-java11.ps1`
    *   **说明**: 执行后当前终端立即生效；重开终端后通过 `JAVA_HOME` 持续生效。

---

**2026-04-04 学生端班级签到对齐教师端风格（4.4 收尾）**

1.  **任务卡片状态展示对齐教师端 Compose 胶囊（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   学生端班级签到列表卡片顶部状态区改为复用教师端 `AttendanceTaskComposeBinder`，统一 `ACTIVE/CLOSED + 时间` 胶囊风格。
        *   保留学生端签到方式标签（基础/定位/手势/密码）并继续按任务要求动态显示。
    *   **影响范围**: `StudentCoursesFragment.java`, `item_sign_in_session.xml`。

2.  **定位签到详情补齐“查看签到范围”地图预览（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   学生提交弹窗中定位任务新增“中心点坐标 + 查看签到范围”入口。
        *   点击后进入高德地图只读预览模式，显示任务中心点与半径覆盖圈，不允许改点提交。
        *   保持提交接口不变，仅增强学生端查看体验。
    *   **影响范围**: `dialog_student_checkin_submit.xml`, `StudentCoursesFragment.java`, `MapPickerActivity.kt`。

3.  **学生提交前校验增强（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   若任务要求手势/密码，学生未填写时前置拦截并提示，不再提交空字段请求。
        *   保持定位自动上送逻辑，增加提交保护，避免定位回调造成重复提交。
    *   **影响范围**: `StudentCoursesFragment.java`。

4.  **验证（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   IDE 诊断检查通过（无新增错误）。
        *   受环境 `JAVA_HOME` 影响，终端编译需在本机设置后执行：`.\gradlew :app:assembleDebug --no-daemon`。

---

**2026-04-04 学生端签到页 UI 与刷新策略优化（4.4）**

1.  **班级签到卡片样式与信息层级优化（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   学生端“班级签到”列表移除左右偏移聊天流展示，统一为常规卡片流，观感对齐教师端考勤任务卡片风格。
        *   卡片右上角由固定“班级签到”改为任务状态胶囊（如 `ACTIVE/CLOSED`），并新增签到方式标签区。
        *   签到方式标签按任务真实要求动态展示：基础签到、定位签到、手势签到、密码签到。
    *   **影响范围**: `StudentCoursesFragment.java`, `item_sign_in_session.xml`。

2.  **学生签到弹窗按任务要求动态表单（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   保留“从底部弹出 + 背景虚化”交互，优化上沿圆角与内容层级。
        *   弹窗改为“按任务要求显示字段”：仅当教师任务配置了手势/密码/定位时显示对应输入或提示，不再一股脑要求学生填写全部字段。
        *   手势签到接入九宫格 Compose 面板（蓝色主题保持不变），并与提交字段联动。
        *   定位签到在弹窗中明确显示为必需项，提交时继续沿用自动定位上送逻辑（接口语义不变）。
    *   **影响范围**: `StudentCoursesFragment.java`, `dialog_student_checkin_submit.xml`, `StudentCheckinComposeBinder.kt`(新增)。

3.  **学生端刷新策略补齐（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   班级签到页新增下拉刷新（`SwipeRefreshLayout`），下拉时走强制云端刷新（GET）。
        *   保持既有 TTL 读本地优先策略；非强刷场景不额外增加远端请求。
        *   保持“POST 提交成功后强刷”语义不变，仅补齐页面手动下拉刷新入口。
    *   **影响范围**: `fragment_student_sign_in.xml`, `StudentCoursesFragment.java`。

4.  **构建验证（已完成）**
    *   **变更结论 - 2026-04-04**:
        *   执行 `:app:assembleDebug --no-daemon`，构建通过。
        *   未改动教师端与接口定义，仅学生端页面与交互层变更。

---

未结清任务
1，对于classroom和student很多小方法未连通
2，对于checkintask相关 考勤记录接口未通，目前打算先在home页实现，然后考虑发布任务审核和学生端签到
3，学生端未实现（可能要对应做出差异化）
例如同一个方法在教师端有实现，在学生端可能需要做一些差异处理

现在已完成一些简单接口 classroom student login register接口（未验证）

---
**2026-03-29 考勤接口检查**（已完成）
修改1：getCheckinTaskCountByTeacher 去掉 teacherId 过滤
修改2：loadStatistics() 的 syncAllClassrooms 的 onSuccess 里，嵌套调用 syncCheckinTasks 后再查数

**核心问题**: Android端数据模型与数据库Schema严重不匹配，导致高级签到功能无法使用，且现有数据显示不全。

---
**2026-03-31 任务规划**

3.31 基本数据库接口已通，细枝末节修改同时，准备学生端
教师端多一个发布考勤以及围绕相关问题的设置处理
学生端多一个提交签ado接口，在提交签到时增加reason字段，用于记录学生签到时的一些额外信息

---
**2026-04-01 任务规划**

1.  **改造考勤管理页面 (已完成)**
    *   **变更结论 - 2026-04-01**:
        *   **双模式视图**: 实现了“按日期组织”和“按状态组织”两种视图模式的无缝切换。
        *   **UI/UX 统一**: 在“按状态组织”模式下，隐藏了日历，将原有的月份切换箭头复用为筛选条件（全部/仅ACTIVE/仅CLOSED）切换器，并统一了顶部控制栏的布局与字体风格。
        *   **数据分组优化**: “按状态组织”的列表现在会先按状态（ACTIVE/CLOSED）分组，内部再按班级进行二级分组。
        *   **构建问题解决**: 解决了因文件锁定导致的编译卡死问题，并记录了强制清理环境的有效命令。
    *   **影响范围**: `AttendanceFragment.java`, `DatabaseHelper.java`, `AttendanceDayAdapter.java`, `AttendanceCalendarComposeBinder.kt`。

---
**2026-04-02 任务规划**

1.  **重构“发布签到任务”功能 (已完成/接口未完成)**
    *   **目标**: 将旧的 XML 布局彻底重构为基于 Jetpack Compose 和 Material 3 的、功能完备且体验现代化的签到任务发布页面。
    *   **变更结论 - 2026-04-02**:
        *   **UI/UX 现代化**:
            *   使用 Jetpack Compose 和 Material 3 风格，从零构建了包含班级选择、任务标题、时间选择、以及所有高级签到选项（地理位置、手势、密码）的现代化表单界面。
            *   将应用主题色从紫色统一为蓝色系，并确保页面内所有交互元素（如按钮）风格一致。
            *   优化了九宫格手势签到UI，确保待连接的圆点在交互前清晰可见。
        *   **地图功能完善**:
            *   成功集成高德地图SDK，创建了独立的 `MapPickerActivity` 用于地图选点。
            *   实现了打开地图时自动请求定位权限，并在授权后将地图视角居中到用户的当前位置，极大提升了可用性。
        *   **编译与依赖修复**:
            *   通过升级 Compose BOM 版本 (`composeBom = "2024.04.00"`)，彻底解决了 `menuAnchor` 的废弃API警告。
            *   为实现定位功能，正确添加了 Google Play 服务的定位库依赖 (`play-services-location`)。
            *   解决了因依赖和导入问题引发的一系列编译失败，并固化了使用“终极构建修复脚本”进行纯净编译的流程。
    *   **遗留问题**: 页面已能完整收集所有任务参数并构建请求对象，但最终的“确认发布”按钮尚未接入真实的后端API。此接口将在后续阶段打通。
    *   **影响范围**: `PublishTaskActivity.kt`, `MapPickerActivity.kt` (新增), `AndroidManifest.xml`, `app/build.gradle.kts`, `gradle/libs.versions.toml`。

2.  **学生端功能开发 (登录界面逻辑实现)**
    *   **考勤管理页**: 创建学生版的考勤记录页面，学生只能查看与自己相关的签到记录。
    *   **签到任务页**: 设计学生查看和执行签到的页面，UI参考“聊天气泡（chat bubble）”样式。
    *   **签到交互与申诉**: 实现学生端的具体签到操作流程，并提供签到结果的申诉功能。

3.  **远期项目优化 (规划中)**
    *   **项目瘦身**: 简化 `build.gradle` 配置，移除无用依赖，优化代码结构。
    *   **UI现代化**: 逐步将项目迁移至 Jetpack Compose 和 Material 3。
    *   **性能优化**: 减少不必要的网络请求和刷新，考虑引入缓存策略。
    *   **架构调整**: 探索集成 Xray 内核的可能性；将人脸识别模块从客户端剥离，迁移至后端服务实现。


**2026-04-03 登录链路修复**

1.  **Compose 登录联调修复 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   **登录鉴权修复**: 为教师/学生登录请求补齐 `X-API-Key`，修复后端统一鉴权导致的登录失败。
        *   **调用链路修复**: 在 `UserRepository` 登录分支中统一传入 API Key，教师登录恢复可用。
        *   **学生接口补齐**: 在 `worker.ts` 新增 `/api/login/student`，按 `sid + password` 返回学生登录数据与 token 字段。
        *   **构建验证通过**: 按“终极构建修复脚本”执行 `:app:assembleDebug`，结果 `BUILD SUCCESSFUL`。
    *   **影响范围**: `ApiService.java`, `UserRepository.java`, `worker.ts`。

---
**2026-04-03 学生端差异化改造**

1.  **登录成功动画与学生端导航差异 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   登录成功后先播放 `telegram.json` 动画，再进入主页面。
        *   学生端中间凹槽点击改为进入“签到申诉”页面，教师端保持发布任务入口。
    *   **影响范围**: `LoginActivity.kt`, `MainActivity.java`, `StudentAppealActivity.kt`, `AndroidManifest.xml`。

2.  **学生考勤与班级签到数据对齐 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   学生首页移除假数据，改为按本人/本人班级统计。
        *   学生考勤页仅展示本人班级任务，日历红蓝点仅按本人班级计算。
        *   学生“班级签到”页改为任务流视图，支持底部弹窗提交签到与填写 reason。
        *   学生申诉页支持查看本人失败/待审核记录并提交申诉 reason。
        *   新增后端学生查询与申诉接口：`/api/checkin/submissions/my`、`/api/checkin/submissions/{id}/appeal`，并支持提交签到时上送 reason。
    *   **影响范围**: `HomeFragment.java`, `AttendanceFragment.java`, `StudentCoursesFragment.java`, `dialog_student_checkin_submit.xml`, `DatabaseHelper.java`, `ApiService.java`, `CheckinSubmitRequest.java`, `MySubmissionsResponse.java`, `AppealRequest.java`, `worker.ts`, `checkinService.ts`。

3.  **构建验证 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   Android 执行 `:app:assembleDebug` 编译通过（BUILD SUCCESSFUL）。
        *   Web 管理端执行 `npm run build` 构建通过。

**2026-04-03 学生端 GET 刷新策略补齐**

1.  **班级签到页接入 TTL + 强刷 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   学生端“班级签到”页改为 `refreshAllData(forceRefresh)`，默认进入与切页走 TTL 策略，本地优先。
        *   点击刷新按钮、切换班级、提交签到成功后，统一走强制云端刷新（GET）。
        *   保持 POST 提交签到直连云端，不接入缓存语义。
    *   **影响范围**: `StudentCoursesFragment.java`。

2.  **学生首页统计接入 TTL + 条件远端同步 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   学生角色进入首页时，新增 `home_stats_student_{studentId}` 刷新键。
        *   TTL 内优先本地统计，TTL 到期或下拉刷新时触发签到任务 GET 同步并回填本地后再刷新统计。
        *   网络失败时保底显示本地统计，避免界面空白。
    *   **影响范围**: `HomeFragment.java`。

3.  **构建修复与验证 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   补齐 `SwipeRefreshLayout` 依赖，修复 `androidx.swiperefreshlayout` 编译缺失。
        *   常规 `:app:assembleDebug` 复现 `R.jar` 文件锁后，按构建卡住脚本流程执行清理并使用 `--no-daemon` 重新构建通过。
        *   最终构建结果：`BUILD SUCCESSFUL`。
    *   **影响范围**: `app/build.gradle.kts`。

---
**2026-04-03 地图定位与发布任务接口修复**

1.  **地图选点定位按钮优化 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   保留高德内置定位按钮，Compose 额外定位按钮改为左下小角悬浮，减少遮挡。
        *   Compose 定位逻辑优先使用高德地图当前位置，回退到 fused lastLocation，提升可用性。
    *   **影响范围**: `MapPickerActivity.kt`。

2.  **发布任务真实接口对接 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   移除“待接入API，请求已构建”占位提示，改为真实调用 `POST /api/checkin/tasks`。
        *   发布成功仅在接口成功后触发，失败时展示服务端错误，不再出现“先失败提示再成功提示”的假成功。
        *   发布按钮增加提交中状态，避免重复点击。
    *   **影响范围**: `PublishTaskActivity.kt`, `ApiCreateResponse.java`。

3.  **云端返回结构兼容与字段对齐 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   `POST /api/checkin/tasks` 返回同时包含 `ok` 与 `success`，兼容 Android 端创建响应解析。
        *   `types.ts` 的 `CheckinSubmissionRequest` 补充 `reason` 字段，和现有提交语义保持一致。
    *   **影响范围**: `worker.ts`, `types.ts`。

4.  **构建验证 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   Android 执行 `:app:assembleDebug` 构建通过。
        *   Web 管理端执行 `npm run build` 构建通过。

---
**2026-04-03 任务切换：教师端观感与学生提交流程优化**

1.  **教师端考勤按日期列表轻量 Compose 优化 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   保持现有 XML 主题不变，仅在“按日期组织/按状态组织”列表项中小量嵌入 Compose 状态胶囊与时间标签。
        *   列表任务行升级为卡片样式，整体风格保持原有蓝灰体系，信息层级更清晰。
    *   **影响范围**: `AttendanceDayAdapter.java`, `AttendanceFragment.java`, `item_checkin_task.xml`, `AttendanceTaskComposeBinder.kt`。

2.  **学生端提交失败链路与答案可见性优化 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   学生提交签到时自动尝试携带当前位置（已授权时），降低位置校验任务的提交失败概率。
        *   提交失败提示改为透传服务端错误信息，不再只显示泛化“提交失败”。
        *   学生“查看已提交”详情可见本人当次手势答案、密码答案与原因，便于自查。
    *   **影响范围**: `StudentCoursesFragment.java`, `MySubmissionsResponse.java`, `worker.ts`。

3.  **云端考勤任务详情增加密码答案显隐 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   在任务详情审核队列中新增“密码答案”密码态展示，并支持小眼睛显隐切换。
        *   保持现有布局结构与视觉风格，仅做局部信息增强，不改变整体 UI 一致性。
    *   **影响范围**: `Attendance.tsx`。

4.  **构建验证 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   Android 常规构建复现 `R.jar` 文件锁后，按清理流程执行并使用 `--no-daemon` 重建通过。
        *   Android `:app:assembleDebug` 最终通过，Web `npm run build` 通过。

---
**2026-04-03 需求澄清后修复：网页详情/教师端查看/学生端时间校验**

1.  **网页端任务详情补齐“发布后可见”的签到方式信息 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   云端考勤任务详情中新增“签到方式”信息区，展示地理位置开关、手势答案、签到密码。
        *   签到密码保持密码态显示，并提供小眼睛显隐切换，整体样式延续原面板风格。
    *   **影响范围**: `Attendance.tsx`。

2.  **教师端手机考勤列表增加“查看详情”入口 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   在考勤管理卡片中新增蓝色“查看”小眼睛入口（同风格轻量按钮）。
        *   仅当任务发布者为当前教师时显示，点击可查看该任务签到方式、时间与对应密码/手势详情。
    *   **影响范围**: `AttendanceFragment.java`, `AttendanceDayAdapter.java`, `item_checkin_task.xml`。

3.  **学生端“时间窗口误判”问题修复 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   后端签到时间校验改为统一解析策略：带时区按原值解析，无时区按 `+08:00` 解析，消除 `Check-in is not within the allowed time frame` 的时区误判。
        *   学生提交失败提示保留服务端原始错误，便于直接定位失败原因。
    *   **影响范围**: `checkinService.ts`, `StudentCoursesFragment.java`。

4.  **构建验证 (已完成)**
    *   **变更结论 - 2026-04-03**:
        *   Android `:app:assembleDebug`（`--no-daemon`）构建通过。
        *   Web 管理端 `npm run build` 构建通过。
