2026-04-07 头像链路对齐优化（Android + 云端，已完成）
本轮完成：

Android 头像上传优化：
接入圆形裁剪流程（拍照/相册 → 拖拽缩放裁剪 → 确认上传）。
新增 AvatarCropActivity.java、CircleCropOverlayView.java 等组件。

头像 Key 与覆盖规则统一：
优先复用已有 avatarUri 进行覆盖，不再每次生成随机路径。
无头像时按规则生成 avatars/{role}/{id}_{nameSlug}_{timestamp}.png，上传后回写数据库。
学生端头像上传链路完全打通，与教师端保持一致。

云端回写确认：
POST /api/profile/avatar 与 POST /api/student/profile/avatar 均在 R2 上传成功后执行 D1 UPDATE 回写 avatarUri。
云端统一使用 normalizeAvatarKey 生成/规范化路径。


影响范围：ProfileFragment.java、ProfileRepository*.java、ProfileViewModel.java、worker.ts 等。
构建验证：

FaceCheck：:app:assembleDebug --no-daemon 通过
omniattend-core：npm run build 通过

待执行：部署最新 Worker + 回归四类头像场景（教师/学生，已有/无头像）。

2026-04-06 人脸识别迁移至云端全流程规划（仅规划）
规划要点：

目标：人脸特征提取与比对完全迁移到云端 Worker，Android 端仅负责采集、压缩、上传照片并提交 photoKey/photoUri。
不影响现有链路：定位、手势、密码、人工审核继续可用。
架构划分：
Android：faceRequired=1 时触发采集 → 上传 R2 → 提交证据键。
Worker：接收提交后执行比对，写入 faceVerifyScore/faceVerifyPassed，驱动 autoResult/finalResult。
审核端：复用现有队列展示照片与分数。


分阶段计划（P0~P3）：

P0：协议冻结、兼容策略、任务级开关。
P1：云端服务分层（faceEnrollService、faceVerifyService、facePolicyService）。
P2：Android 提交流程切换 + 离线重试。
P3：监控、异常分级、灰度与回滚。

证据存储约定：records/{taskId}/{studentId}/{timestamp}_{uuid}.jpg（私有访问）。
验收标准：人脸必选任务闭环、审核端可见证据、旧客户端兼容、关键路径可重试。

2026-04-06 学生端签到缓存与地图选点优化（已完成）

StudentCoursesFragment 改为先读本地缓存展示，再按 TTL 异步同步远端，解决二次进入需手动刷新问题。
地图选点优化：初次自动定位仅移动视角，不覆盖用户已手动选点。
构建通过。


2026-04-05 学生端“去签到”弹窗优化 + 人脸上云准备（已完成）
弹窗优化：

使用 NestedScrollView + 调整手势面板高度（240dp），支持定位+手势+密码并发场景滚动。
BottomSheetDialog 默认展开，减少拥挤与误触。

人脸上云准备：

CheckinSubmission 最小增列 photoKey、photoUri、faceVerifyScore、faceVerifyPassed（含迁移脚本）。
新增学生资料接口：PUT /api/student/profile/username、PUT /api/student/profile/password（支持旧密码校验），学生端资料页打通。

人脸迁移首步：

新增 services/face/faceEmbeddingService.ts，抽离 FaceEmbedding 逻辑，为后续云端化做准备。

首页预加载：

教师/学生端 Home 页先读本地统计秒开，再异步远端同步覆盖。

构建验证：Android 与 omniattend-core 均通过。

2026-04-04 ~ 2026-04-05 地图与发布任务修复（已完成）

地图选点稳定性优化：首轮确认兜底使用当前中心点，逆地理失败时坐标兜底展示。
发布任务接口完全打通（POST /api/checkin/tasks）。
学生端签到弹窗闪退修复 + 密码答案显隐切换。
教师端考勤列表增加“查看详情”入口。


2026-04-03 学生端差异化改造与登录修复（已完成）

学生端登录成功动画 + 导航差异（凹槽进入签到申诉页）。
学生端首页、考勤页、班级签到页全部去除假数据，改为真实本人/本人班级数据。
学生“去签到”支持手势面板 + 动态表单（按任务要求显示字段）。
补齐学生端 TTL 缓存 + 下拉强刷策略。
云端新增学生登录接口 /api/login/student + 提交 reason 字段支持。
任务详情页补齐签到方式信息（位置/手势/密码）并支持密码显隐。

构建验证：Android 与 Web 端均通过。