<?xml version="1.0" encoding="utf-8"?>
<!-- 与学生端 dialog_student_checkin_submit 一致：大圆角卡片 + 顶条，避免单层 FrameLayout+Compose 在 BottomSheet 内测量异常 -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="28dp"
    app:cardElevation="0dp"
    app:cardUseCompatPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="12dp"
        android:paddingEnd="20dp"
        android:paddingBottom="20dp">

        <View
            android:layout_width="44dp"
            android:layout_height="5dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="14dp"
            android:alpha="0.35"
            android:background="@drawable/edit_text_background" />

        <androidx.compose.ui.platform.ComposeView
            android:id="@+id/compose_classroom_checkin_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="120dp"
            android:maxHeight="@dimen/classroom_checkin_status_compose_max_height" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
