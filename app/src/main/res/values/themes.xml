<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FaceCheck" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 主色调 -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="colorSecondary">@color/teal_200</item>
    </style>
    
    <!-- AppBar Overlay Theme -->
    <style name="Theme.FaceCheck.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />
    
    <!-- Popup Overlay Theme -->
    <style name="Theme.FaceCheck.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
    
    <!-- NoActionBar Theme -->
    <style name="Theme.FaceCheck.NoActionBar" parent="Theme.FaceCheck">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    
</resources>